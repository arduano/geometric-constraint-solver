<!-- SPDX-License-Identifier: GPL-3.0-or-later -->

# M87 closeout handover — historical pause and final disposition

Status: **M87 accepted and closed on 2026-08-31; this former pause handover is superseded by
`docs/M88_HANDOVER.md`**. Exact product source
`32c72892772ee09f8b904153484b02fd9923dc25`, tree
`38f7175f93c87d11422f5de00e78208f8cf315bb`, passes the complete clean release gate. The user's
milestone-level close decision accepts M87-U9/U10 without claiming a separately logged row-by-row
visual replay. No immutable candidate, publication or service retirement is claimed. M88 followed
and is now complete.
The remaining dirty-tree material below is retained only as historical pre-close provenance.

## Brief supervising-user consolidation — 2026-08-30

The additional agent session was accidental and does not authorize a separate milestone or broad
cleanup. Its two useful sample additions were explicitly retained inside then-open M87:

- **CNC joinery fit coupon · keyed corner reliefs**, including its `cornerReliefs` patch/artifact,
  package mirrors, managed fixture, catalog/ledger integration and focused manufacturing coverage;
- **Gridfinity 1×1×3U section · keyed standard profile**, including its notice, managed fixture,
  catalog/ledger integration and focused manufacturing coverage.

Preserve those sample sources, fixtures, tests and integration changes. The supervising user
confirmed that this handover's repository summary was accurate at that checkpoint. The later
requested revisions and final closeout are recorded above.

The expected sample revision is now **M87-F003**: CNC replaces seven unrelated component
`FixedPoint` locks with one absolute point plus seven relational construction datums; Gridfinity
replaces an unconstrained 26-point literal-seed contour with one scalar Y datum, thirteen mirror
relations and ten dimensioned orthogonal projection spans. Nominal geometry, topology, generated
Fillets and manufacturing meaning remain unchanged. Both sketches must independently prove zero
numerical/structural nullity and zero equality/bidirectional bounded DOF.

## Historical pre-close resume point

- Repository/worktree: `/home/arduano/programming/geometric-constraint-solver`.
- Branch: `main`.
- HEAD: `1ea4940550e386956783a81c983a17e9705fc43d`.
- `origin/main...HEAD`: `0 0` at handover preparation; all M87 work is intentionally uncommitted in
  the shared dirty worktree.
- Expected final status: 50 tracked/index-visible modified paths and 34 untracked paths, including
  this handover. The 33 other untracked paths are implementation, tests, fixtures, licences and M87
  authority documents—not disposable build output.
- No paths are staged.
- At this historical checkpoint no commit, reset, clean, stash, checkout, push, publication,
  service change or M88 activation was authorized. The later M87 implementation commit and closeout
  supersede that pause instruction.

Before M87-F003, after this file and its `START_HERE.md` link were added, the exact handover
signatures were:

- tracked binary diff SHA-256: `c16299d3a998687c614e43a4cb64d19d28eea447d3c65172baacd488c7b89494`;
- untracked payload-manifest SHA-256 excluding this self-describing handover:
  `821f8a27c9bd51d387828ecc6d809e584ec3f6863838cad7ce21c0f2692e1a20`;
- complete porcelain-status path/state SHA-256, including this handover:
  `aaa3df3ccb3eca8f899af25226c7a185901a935a2ebcbd0592252cba3ae268c5`.

The verification commands and expected counts are retained below as a historical checkpoint. They
must differ after the authorized F003 sample/test/ledger/documentation edits and are no longer a
current-tree oracle.

## Historical pause procedure — superseded

1. `cd /home/arduano/programming/geometric-constraint-solver`.
2. Read `AGENTS.md` completely, then its required `START_HERE.md`, `ARCHITECTURE.md`, `PLAN.md`,
   `ACCEPTANCE.md` and `docs/SCENARIOS.md` project records.
3. Read this handover completely.
4. Read `docs/adr/0042-managed-controls-and-headless-authoring.md`, `docs/M87_GOALS.md`,
   `docs/M87_AUDIT.md`, `docs/M87_IMPLEMENTATION.md`, `docs/M87_HEADLESS.md` and
   `docs/M87_UAT.md` completely.
5. Verify the repository identity and inspect the current dirty tree. Treat the checkpoint hashes
   below as historical pre-F003 evidence, not expected current values.
6. Confirm the recorded post-F003 focused qualification and fresh mutable bundles still exist. If
   the `/tmp` products are unavailable, render both revised demos into new, non-existing child
   directories under a new `mktemp -d` parent; never overwrite an earlier generation or call the
   replacement immutable or accepted.
7. At the pause, resume with M87-U9 and M87-U10 when the supervising user is available for visual
   review.
8. This procedure is now historical: the later milestone-level close decision and clean gate own
   final disposition. `docs/M88_HANDOVER.md` records the subsequently completed milestone.

## Authority boundary to preserve

- Managed-control derivation, exact-CAS source edits, generated-consumer fan-out and outer history
  are optional `geosolve-sketch-code`/workbench concerns.
- Solver-instance point movement stays on the existing draft/semantic-overlay route and never
  writes solved coordinates into `sketch.ts`.
- Keyed reconciliation/topology, native materialization, computed Fillets and rendering retain
  separate owners. The reusable `cornerReliefs` patch knows only caller-owned keyed centres and a
  radius; it has no coupon-layout, widget, renderer or LOD knowledge.
- `geosolve-sketch-render` consumes independently accepted scene authority. It owns no solver,
  persistence, managed-control, selection or hit-ranking state.
- The entire experimental adaptive-detail/LOD prototype remains deleted. Camera navigation always
  retains the complete scene paint; completed M88 did not restore LOD.
- The CNC and Gridfinity entries are 2D/2.5D design sketches only. They claim no CAM, toolpath,
  automatic cutter compensation, boolean, solid, print-fit, machinability or production-
  manufacturing authority.
- Reported performance work is deliberately deferred to a follow-up milestone. It is not a reason
  to broaden this paused UAT handoff.

## Delivered M87 work

- A bounded transient managed-control manifest authenticates code-owned non-DoF source values,
  schemas, exact source spans and complete semantic consumer fan-out. Shared controls edit one
  token in one outer transaction; stale, foreign, dirty, wrong-unit, overlapping and over-bound
  batches fail closed.
- Inspector, Code panel, grouped generated-Fillet grip and strict code-control RPC use the same
  source authority while ordinary GUI controls and point overlays retain their previous owners.
- `geosolve-headless` provides browser-free inspect, exact-CAS edit and deterministic render APIs/
  CLI. `geosolve-sketch-render` provides shared target-neutral camera/SVG composition and native
  pure-Rust PNG with bundled resources.
- The tenth bundled project is the robotic routing board: eight keyed routes, 80 clips, 64 host
  Fillets, shared/local controls, one solver-instance service loop and keyed structural insertion.
- The eleventh project is the CNC joinery fit coupon: one 120 x 140 mm blank, three 70 mm-wide
  mortises with loose/nominal/press heights 18.4/18.0/17.6 mm, three 95 x 18 mm tabs, twelve
  shared-radius keyed corner-relief circles and ten handling Fillets.
- The twelfth project is one symmetric closed 26-point Gridfinity 1 x 1 x 3U central material
  section with reviewed 41.5/35.6 mm widths, 4.75/7 mm base levels, 21 mm body, 0.95 mm walls,
  4.4 mm nominal lip rise and independent two-floor/two-lip Fillet controls.
- Post-F003 accepted native inventories, in declaration/generated/output/point/curve/constraint/
  host-output/feature/computed-edge order, are CNC
  `(62, 69, 14, 29, 47, 36, 10, 10, 23)` with 33 dimensions and Gridfinity
  `(62, 66, 3, 31, 36, 31, 4, 4, 11)` with 18 dimensions. CNC owns exactly one `FixedPoint` and no
  `FixedCoordinate`; Gridfinity owns no `FixedPoint` and one Y `FixedCoordinate`.

## Historical pre-F003 mechanical qualification

The following focused additive qualification passed for the original under-constrained/fixed-lock
sample sources and does not qualify M87-F003:

- `m87_manufacturing_sketches`: 3/3;
- `m87_headless`: 10/10, including all twelve bundled demos and deterministic manufacturing
  products;
- `m84_native_composition`: 13/13;
- exact separately reviewed code-project ledger: 1/1.

The then-current pre-F003 dirty-worktree release gate ran with:

```bash
TMPDIR=/home/arduano/.cache/geosolve-m87-tmp \
GEOSOLVE_ALLOW_DIRTY=1 NO_COLOR=true \
nix-shell shell.nix --run \
'TMPDIR=/home/arduano/.cache/geosolve-m87-tmp ./scripts/release-gate.sh'
```

It exited `0`, including formatting/diff hygiene, warnings-denied workspace Clippy, all-feature
workspace tests, browser 344/344, headless 10/10, Rustdoc, locked WASM and actual WASM exports,
both TypeScript packages, unchanged-golden checks, licence/package verification, release
performance checks and final Trunk assembly. This terminal run was not retained as a standalone log
file; `docs/M87_IMPLEMENTATION.md` records its outcome. The gate explicitly allowed a dirty
worktree and is mechanical evidence only.

The post-F003 reviewed twelve-row ledger is
`crates/geosolve-sketch-code/tests/golden/m84_code_project_ledger.tsv`, SHA-256
`f6ecd037cef8befc59f9a057fef499a14f0851f8ec5a0d3a1468a69e66a9d1bc`. The old gate predates this
ledger revision. The separate post-F003 focused checks below proportionally qualify the revised
samples without converting that historical gate into current full-workspace qualification.

Final post-F003 qualification: `m87_manufacturing_sketches` passes 3/3, including zero nullity/DOF and minimal
datum assertions; native composition passes 13/13, the exact reviewed-ledger check passes 1/1 and
all-demo headless/deterministic products pass 10/10. Fresh visual bundles are under
`/tmp/geosolve-m87-post-f003.FPHP3b`. Exact source `32c7289`, tree `38f7175`, passes the complete
clean release gate; the user's milestone-level close decision accepts U9/U10 without a separate
replay.

## Historical pre-F003 mutable UAT evidence

Parent: `/tmp/geosolve-m87-manufacturing-uat.K7YRaV`.

| Row | Directory | Validation | SVG SHA-256 | PNG SHA-256 |
|---|---|---|---|---|
| M87-U9 CNC | `cnc` | hard residuals independently valid; max `7.105427357601002e-15`; all 10 active features Current | `b786a48f7b2bbddbb3c9ef082143af0ae2b4f9b6ec77d31d899f48317e1969f7` | `e7a474ca4d13aa9bdaba4b512acbf72c2ea2f2b907d6127d173439ec5c8f1f66` |
| M87-U10 Gridfinity | `gridfinity` | hard residuals independently valid; max `0`; all 4 active features Current | `fd3b3648b289d9268acd52309bf7f02eabd2d7925eb24121dae90515840c41f3` | `c4caf34bae19beba2138eb0ceba9c0edf6163351a2abfcecfee9b0745bad00e9` |

Each directory contains exactly `controls.json`, `project.json`, `report.json`, `scene.png`,
`scene.svg` and `sketch.ts`; both PNGs are 2000 x 1400. These directories are mutable review
evidence for the old sources, not a clean, frozen, immutable, current or accepted candidate.

Current fresh post-F003 replacement evidence is under
`/tmp/geosolve-m87-post-f003.FPHP3b/{cnc,gridfinity}`. If it is unavailable, regenerate it with:

```bash
evidence_root=$(mktemp -d /tmp/geosolve-m87-manufacturing-uat.XXXXXX)
cargo run --locked -p geosolve-headless -- \
  render --demo cnc-joinery-fit-coupon --out "$evidence_root/cnc"
cargo run --locked -p geosolve-headless -- \
  render --demo gridfinity-1x1x3-section --out "$evidence_root/gridfinity"
```

Record any new paths as mutable dirty-UAT evidence only.

## Historical focused human rows — resolved by milestone disposition

- **M87-U9:** open **CNC joinery fit coupon · keyed corner reliefs**, fit the complete scene, compare
  against the fresh SVG/PNG, inspect shared cutter/handling controls and visually distinguish loose,
  nominal and press stations. Use the exact pass condition in `docs/M87_UAT.md`.
- **M87-U10:** open **Gridfinity 1×1×3U section · keyed standard profile**, fit the complete scene,
  compare against the fresh SVG/PNG and inspect independent floor/lip controls. Use the exact pass
  condition in `docs/M87_UAT.md`.

The post-F003 focused rank/DOF, inventory, ledger and headless qualification is complete and fresh
mutable review bundles exist. The 2026-08-31 supervising-user close decision accepts both rows at
milestone level without claiming a separately executed visual replay. M87 is closed; use
`docs/M88_HANDOVER.md` for M88's completed closeout record.

## Historical dirty-tree verification

Run from the exact repository root:

```bash
test "$(git rev-parse --show-toplevel)" = \
  /home/arduano/programming/geometric-constraint-solver
test "$(git rev-parse --abbrev-ref HEAD)" = main
test "$(git rev-parse HEAD)" = 1ea4940550e386956783a81c983a17e9705fc43d
test "$(git rev-list --left-right --count origin/main...HEAD)" = $'0\t0'

git status --porcelain=v1 | awk '
  BEGIN { tracked = 0; untracked = 0 }
  /^\?\?/ { untracked++; next }
  { tracked++ }
  END {
    printf "tracked_or_indexed=%d untracked=%d total=%d\n",
      tracked, untracked, tracked + untracked
  }'

git diff --binary -- . | sha256sum
git ls-files --others --exclude-standard |
  LC_ALL=C sort |
  while IFS= read -r path; do
    test "$path" = docs/M87_HANDOVER.md && continue
    sha256sum -- "$path"
  done | sha256sum
git status --porcelain=v1 | sha256sum
```

The historical pre-F003 count output was `tracked_or_indexed=50 untracked=34 total=84`. The three
hashes matched the values near the top of this document only at that checkpoint. Do not restore or
discard F003 work to reproduce them. `git diff --check` and `cargo fmt --all -- --check` must remain
clean before a new handoff.

## Mutable development listener observed at handover

At handover preparation, PID `3823027` ran
`trunk serve --address 100.94.63.83 --port 8080` from
`/home/arduano/programming/geometric-constraint-solver/crates/geosolve-demo-web` and listened on
`100.94.63.83:8080`. It is a long-lived mutable development listener, not frozen candidate or
publication authority; verify its current PID/argv/bytes before using it and do not infer UAT from
its existence. A separate `code` process listened on `127.0.0.1:8080`; do not confuse it with the
Tailscale listener. This handover authorizes no listener replacement or retirement.

## Active handoff

M87 requires no further implementation or UAT work. Preserve its source, samples, clean-gate
evidence, no-LOD contract and mutable development listener. Resume M88 from
`docs/M88_HANDOVER.md`.
